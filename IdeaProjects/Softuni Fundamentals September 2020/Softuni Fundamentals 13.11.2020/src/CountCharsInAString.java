import java.util.*;
       /*
public class CountCharsInAString {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

 String[] words = scanner.nextLine().charAt(0);

        Map<String, Integer> charsCount = new LinkedHashMap<>();
        for (int i = 0; i < words.length - 1; i++) {
            for (String word : words) {
                if (!charsCount.containsKey(word)) {
                    charsCount.put(word, 0);
                }
                charsCount.put(word, charsCount.get(word) + 1);
            }
            }
        for (Map.Entry<String, Integer> entry : charsCount.entrySet()) {
            System.out.printf("%s -> %d%n", entry.getKey(), entry.getValue());
        }
    }
}
*/